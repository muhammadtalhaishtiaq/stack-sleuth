# bob_sessions — Bob IDE task evidence

Every Bob IDE task session summary screenshot for this submission goes here as a PNG:

```
<team>_taskNN_<short-description>.png
```

Example: `muhammadtalha_task01_reproduce_failure_summary.png`

Take each screenshot from the Bob IDE Tasks view (select "All" if tasks span workspaces), right after the task summary appears. These screenshots are required evidence of Bob IDE usage for judging eligibility.
